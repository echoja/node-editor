import type { Config } from "tailwindcss";

// Tailwind v4: config is optional. Keep minimal file for tooling compatibility.
export default {
  content: [],
  theme: { extend: {} },
  plugins: [],
} satisfies Config;

